// Fixed, full-page pink gingham (checkered) background.
// Mounted once in App.jsx, sits behind every page.
export default function GinghamBackground() {
  return <div className="gingham-bg" aria-hidden="true" />;
}
